
import { resultsFiltered } from 'frontend/helpers/results-filtered';
import { module, test } from 'qunit';

module('Unit | Helper | results filtered');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resultsFiltered([42]);
  assert.ok(result);
});

