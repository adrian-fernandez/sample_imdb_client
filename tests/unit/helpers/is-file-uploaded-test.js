
import { isFileUploaded } from 'frontend/helpers/is-file-uploaded';
import { module, test } from 'qunit';

module('Unit | Helper | is file uploaded');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isFileUploaded([42]);
  assert.ok(result);
});

