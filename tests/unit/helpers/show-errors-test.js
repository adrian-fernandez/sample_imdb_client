
import { showErrors } from 'frontend/helpers/show-errors';
import { module, test } from 'qunit';

module('Unit | Helper | show errors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = showErrors([42]);
  assert.ok(result);
});

